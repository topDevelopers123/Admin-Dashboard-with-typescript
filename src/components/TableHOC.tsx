import { useTable, Column } from "react-table";

function TableHOC<T>(
  column: Column[],
  data,
  containerClassName: string,
  heading: string
) {
  return function HOC() {};
}

export default TableHOC;
